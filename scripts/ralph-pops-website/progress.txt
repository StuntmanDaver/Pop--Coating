# Ralph Progress Log — Pops Website Rebuild
Started: (not yet — set by ralph.sh on first run)
Branch: ralph/pops-website
PRD: scripts/ralph-pops-website/prd.json
Comprehensive PRD: docs/prd/POPS-WEBSITE-REBUILD.md

## Codebase Patterns

- **Turbopack monorepo middleware shadowing**: Next.js 16 + Turbopack uses the pnpm workspace root as the project base for middleware discovery. It finds `src/proxy.ts` at the monorepo root (the SaaS app's middleware) when building pops-website. Fix: create `apps/pops-website/src/proxy.ts` as a no-op stub to shadow the root's middleware for this workspace package.
- All page content sources from .firecrawl/popsindustrial/pages/<slug>.md
- content/company.ts is the single source of truth for business facts
- Footer is rendered from content/site-globals.json#footer.navigation_columns
- Shipping ZIP locked at 33811 (open question §12.1 unresolved)
- Logo canonical: /images/Pops-no-border.png
- pops-website uses `apps/pops-website/app/` for routes (NOT `src/`) — no src-dir convention here
- pops-website .gitignore must exclude node_modules, .next, *.tsbuildinfo to avoid committing build artifacts
- Tailwind v4 is CSS-first: `@import "tailwindcss"` + `@theme { ... }` in app/globals.css; postcss.config.mjs uses `@tailwindcss/postcss`. NO tailwind.config.ts.
- Design tokens (color-pops-yellow-*, color-ink-*, semantic, font-display/text/mono, radius-*, shadow-1..4) are sourced VERBATIM from docs/design/popsindustrial-design-principles.md §10. Do NOT diverge — those values are paint-chip-locked.
- Tailwind v4 auto-generates utility classes from `@theme` tokens (e.g. `bg-ink-800`, `text-pops-yellow-500`, `font-display`, `rounded-sm`, `shadow-3`) — no need to declare them.
- next/font CSS variables (`--font-display`, `--font-text`) are set on `<html>` via the className spread; they CASCADE-OVERRIDE the static fallback stacks declared in `@theme`. The `font-display` / `font-text` Tailwind utilities resolve `var(--font-*)` at the element, so the next/font self-hosted family wins. Self-hosted assets land at `/_next/static/media/*.woff2` — never the Google Fonts CDN.
- Static assets are in `apps/pops-website/public/` (URL: `/images/<file>`, `/pdfs/<file>`, `/favicon.ico`). Server-side content imports live at `apps/pops-website/content/` (e.g. `content/site-globals.json` for footer / nav columns). NEVER serve content/site-globals.json over HTTP — it's an `import` target only.
- Source of static assets: `.firecrawl/popsindustrial/assets/{images,pdfs}/` — 194 images + 4 PDFs. If a story needs an image, check this directory first; the file is already at `/images/<exact-filename>` after US-004.
- Multi-resolution `favicon.ico` was generated via `magick <png> -define icon:auto-resize=16,32,48,64,128,256 favicon.ico`. Re-run that command if `cropped-favicon.png` is ever updated.

---

## Story Status (49 stories total)

Phase A — Workspace + foundation (US-001 to US-005): 5/5 complete
Phase B — Design primitives (US-006 to US-016): 0/11 complete
Phase C — Page implementations (US-017 to US-034): 0/18 complete
Phase D — Forms + integrations (US-035 to US-039): 0/5 complete
Phase E — SEO + accessibility (US-040 to US-044): 0/5 complete
Phase F — Quality + ship (US-045 to US-049): 0/5 complete

OVERALL: 5/49 stories passing

---

## 2026-05-02 — US-001 Add apps/pops-website pnpm workspace + Next.js 16 scaffold
- Created workspace scaffold with package.json (name: pops-website, scripts: dev/build/start/type-check/lint)
- next.config.ts with images.formats: ["image/avif", "image/webp"]
- tsconfig.json with strict: true, noUncheckedIndexedAccess, noImplicitOverride
- app/layout.tsx with Metadata (title/description) and basic RootLayout
- app/page.tsx with placeholder home content
- app/globals.css with comment deferring Tailwind to US-002
- .gitignore excluding node_modules, .next, *.tsbuildinfo
- src/proxy.ts no-op middleware stub (see Codebase Patterns above)
- pnpm-workspace.yaml already had "apps/*" — verified unchanged
- Files changed: apps/pops-website/.gitignore, app/globals.css, app/layout.tsx, app/page.tsx, next.config.ts, package.json, src/proxy.ts, tsconfig.json
- Quality checks: typecheck ✓ | build ✓ | browser N/A (no UI yet)
- **Learnings for future iterations:**
  - Turbopack (Next.js 16) discovers middleware relative to the pnpm workspace root, not the package directory. The fix is a `src/proxy.ts` stub in pops-website that passes through all requests. This MUST exist for every Next.js app in this monorepo unless proper workspace isolation is configured.
  - pops-website has no `src/` convention — routes live in `apps/pops-website/app/` directly. The `src/proxy.ts` stub file is solely for the Turbopack workaround.
  - The `@/*` path alias in pops-website tsconfig maps to `./src/*` (i.e., `apps/pops-website/src/*`) — this is correct and consistent with the SaaS app convention, but note that pops-website's src only contains the proxy stub, not actual app code.
---

## 2026-05-02 — US-002 Add Tailwind v4 + design tokens to apps/pops-website/app/globals.css
- Added tailwindcss@^4.2.4 + @tailwindcss/postcss@^4.2.4 to devDependencies via `pnpm --filter pops-website add -D`
- Wrote apps/pops-website/postcss.config.mjs registering `@tailwindcss/postcss` plugin
- Replaced apps/pops-website/app/globals.css placeholder with:
  - `@import "tailwindcss"` directive
  - `@theme { ... }` block VERBATIM from docs/design/popsindustrial-design-principles.md §10:
    - 5 brand colors (pops-yellow 100/300/500/600 + tan-400)
    - 11 ink/paper neutrals (ink-50..900 + paper)
    - 8 semantic colors (success/warn/danger/info — 100/500 pairs)
    - 3 font tokens (display/text/mono)
    - 5 radius tokens (none/sm/md/lg/full)
    - 4 elevation tokens (shadow-1..shadow-4)
  - `html { background: var(--color-ink-800); color: var(--color-ink-100); }` per AC
  - `@media (prefers-reduced-motion: reduce)` block disabling animations/transitions per design principles §8
- Files changed: apps/pops-website/app/globals.css, apps/pops-website/postcss.config.mjs, apps/pops-website/package.json, pnpm-lock.yaml
- Quality checks: typecheck ✓ | build ✓ (Compiled successfully in 1886ms; 3/3 static pages generated)
- **Learnings for future iterations:**
  - Tailwind v4 auto-derives utility class names from `@theme` token names. e.g. `--color-pops-yellow-500: #FECD08` automatically yields `bg-pops-yellow-500`, `text-pops-yellow-500`, `border-pops-yellow-500`, etc. NO `tailwind.config.ts` required, NO `theme.extend` block.
  - For radius and shadow, the same auto-mapping applies: `--radius-sm` → `rounded-sm`, `--shadow-3` → `shadow-3`. Future component stories can reference these utility classes directly.
  - For fonts, `--font-display` → `font-display`, `--font-text` → `font-text`. The font *files* will land in US-003 (next/font wiring); §10 tokens declare the FALLBACK stacks only.
  - The `html` element styles in globals.css apply globally before next/font wires up CSS vars (US-003), so initial paint is dark + light text — no FOUC on the dark theme.
  - Build via Turbopack pulls the @theme block + reduced-motion media query into the final CSS automatically; nothing extra was needed in next.config.ts.
---

## 2026-05-02 — US-003 Wire next/font Archivo Black + Inter via app/layout.tsx
- Updated apps/pops-website/app/layout.tsx to:
  - Import `Archivo_Black` and `Inter` from `next/font/google`
  - Configure Archivo_Black with `weight: "400"`, `subsets: ["latin"]`, `variable: "--font-display"`, `display: "swap"`
  - Configure Inter with `subsets: ["latin"]`, `variable: "--font-text"`, `display: "swap"`
  - Set `<html className={`${display.variable} ${text.variable}`}>` so next/font injects both CSS vars
  - Set `<body className="font-text">` so the Tailwind v4 utility maps to `var(--font-text)` (the next/font value wins via cascade)
- Updated apps/pops-website/app/page.tsx so the placeholder `<h1>` carries `className="font-display"` (lets the browser-verification step prove Archivo Black actually renders on the heading)
- Files changed: apps/pops-website/app/layout.tsx, apps/pops-website/app/page.tsx
- Quality checks: typecheck ✓ | build ✓ (Compiled in 2.1s; TS in 1093ms; 3/3 static pages) | browser ✓
- Browser verification (Playwright MCP at http://localhost:3001):
  - `<html>` className contains both `archivo_black_*__variable` and `inter_*__variable` from next/font
  - `getComputedStyle(html).getPropertyValue('--font-display')` → `"Archivo Black", "Archivo Black Fallback"`
  - `getComputedStyle(html).getPropertyValue('--font-text')` → `"Inter", "Inter Fallback"`
  - `<body>` computed font-family: `Inter, "Inter Fallback"` ✓
  - `<h1>` computed font-family: `"Archivo Black", "Archivo Black Fallback"` ✓
  - `<p>` computed font-family: `Inter, "Inter Fallback"` ✓
  - Network: 2 woff2 files, both served from `/_next/static/media/*.woff2` (self-hosted, NOT Google Fonts CDN) ✓
- **Learnings for future iterations:**
  - Per design principles §4.4 the design contract suggests `axes: ["opsz"]` on Inter for crisp small text. The US-003 AC does NOT include `axes`, so I omitted it to keep the contract minimal — a future story (or a §4.4-tightening pass) can opt-in if the visual benefit warrants the extra weight (~30KB per axis). Document this trade-off if revisiting.
  - When both `@theme { --font-text: ... }` (Tailwind v4 fallback stack) AND next/font's `variable: "--font-text"` (self-hosted family) declare the same variable, the next/font value wins because it's set on `<html>` (closer scope than `:root` from @theme). No conflict, no FOUC — Tailwind's `font-text` utility just dereferences `var(--font-text)` at the element and gets whichever value cascades.
  - Putting the className spread on `<html>` (vs `<body>`) means the variables are also available to anything attached directly to `<html>` (e.g. global background color). Per the AC and Next.js docs, this is the recommended placement.
  - Archivo Black is a single-weight font (400 file weight that visually renders as 900); next/font requires the `weight` prop because Archivo Black is non-variable. Inter is variable, so no `weight` prop is needed (next/font ships the full variable file).
---

## 2026-05-02 — US-004 Copy scraped images and PDFs into apps/pops-website/public/
- Created `apps/pops-website/public/{images,pdfs}/` and `apps/pops-website/content/`
- `cp -R .firecrawl/popsindustrial/assets/images/. apps/pops-website/public/images/` → 194 image files
- `cp -R .firecrawl/popsindustrial/assets/pdfs/. apps/pops-website/public/pdfs/` → 4 PDF files
- `cp .firecrawl/popsindustrial/_raw/site-globals.json apps/pops-website/content/site-globals.json`
- Generated multi-resolution `apps/pops-website/public/favicon.ico` from `cropped-favicon.png` via ImageMagick `magick … -define icon:auto-resize=16,32,48,64,128,256` (16/32/48/64/128/256 px sizes embedded in single .ico)
- `.gitignore` already does NOT exclude `public/` or `content/` — verified unchanged
- Per AC notes, `site-globals.json` lives at `content/` (server-side import target), NOT `public/` (would get served verbatim to clients). The "Wait, correct that…" line in the AC is the corrected rule.
- Files changed: apps/pops-website/public/** (194 images + 4 PDFs + favicon.ico), apps/pops-website/content/site-globals.json
- Quality checks: typecheck ✓ | build ✓ (Compiled in 1984ms; 3/3 static pages) | browser N/A (no UI change yet)
- **Learnings for future iterations:**
  - The asset filenames frequently contain quirks from the original site (e.g. typos, locale variants, repeated numeric suffixes like `-1.jpg`, `-150x150.jpg`, `-300x300.jpg`). Use the actual filename present in `public/images/` — do NOT rename or "clean up" filenames unless an explicit story authorizes it. site-globals.json#alt_text_audit references a few of these typos that need preserving for content faithfulness.
  - The 4 PDFs are: Application checklist, Quality checklist, Receiving checklist, Shipping checklist (per `.firecrawl/popsindustrial/assets/pdfs/`). Reference them at `/pdfs/<filename>.pdf`.
  - For favicon, Next.js 16 will surface `app/icon.{png,svg,ico}` via the App Router automatically, but the AC explicitly asked for `public/favicon.ico` — keeping that location works for both legacy `<link rel="icon">` and the default Next.js favicon resolution from `public/`.
  - macOS ships `magick` (ImageMagick 7) at `/opt/homebrew/bin/magick`; preferred over `convert` (legacy alias) and `sips` (no ICO support).
---

## 2026-05-02 — US-005 Build content/company.ts single-source-of-truth file
- Created apps/pops-website/content/company.ts with `as const` company object
- Fields per PRD §7: name, founded (1972), generations (4), mainOffice, shippingReceiving, phone, fax, emails {info, invoices}, hours, tagline, taglineSecondary, services (5)
- mainOffice.zip = "33811", shippingReceiving.zip = "33811" (locked per open question §12.1; live footer says 33813 — flagged with TODO comment)
- Exported `type Company = typeof company` for downstream consumers (footer US-013, contact US-031, JSON-LD US-041)
- Files changed: apps/pops-website/content/company.ts (new file)
- Quality checks: typecheck ✓ | build ✓ (Compiled in 1987ms; 3/3 static pages) | browser N/A (no UI yet)
- **Learnings for future iterations:**
  - tsconfig.json `include: ["**/*.ts", "**/*.tsx"]` already covers `content/` so `content/company.ts` is type-checked without any include changes — same applies to future server-side imports under content/.
  - Keep `as const` on the entire object so consumers get literal types (e.g. `company.services[0]` is the literal "Wet Paint Coatings", not `string`). This matters for type-safe footer columns, JSON-LD `@type: "Service"` arrays, and Zod refinements that pin allowed service names.
  - The hours string uses an em dash (–) for "Monday–Friday 8am–4pm" matching the PRD §7 source verbatim — don't normalize to ASCII; copy faithfulness is part of the brand voice.
---

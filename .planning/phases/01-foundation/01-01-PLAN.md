---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - eslint.config.js
  - .gitignore
  - .env.local.example
  - components.json
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/(office)/layout.tsx
  - src/app/(office)/page.tsx
  - src/app/(portal)/layout.tsx
  - src/app/(portal)/page.tsx
  - src/app/scan/page.tsx
  - src/i18n.ts
  - src/messages/en/common.json
  - src/messages/en/auth.json
  - src/shared/ui/button.tsx
  - src/shared/ui/input.tsx
  - src/shared/ui/label.tsx
  - src/shared/ui/card.tsx
  - src/shared/ui/alert.tsx
  - src/shared/ui/form.tsx
autonomous: true
requirements: [INFRA-01, INFRA-07]

must_haves:
  truths:
    - "pnpm install completes without errors and produces a working node_modules tree"
    - "pnpm dev starts Next.js 16 and serves a page at app.localhost:3000 and track.localhost:3000"
    - "pnpm type-check passes with TypeScript strict mode enabled"
    - "pnpm lint passes; ESLint flat config blocks deep module imports and gates @/shared/db/admin to allowed modules only"
    - "Tailwind v4 CSS-first config in src/app/globals.css renders the brand color tokens defined in 01-UI-SPEC.md"
    - "shadcn/ui components button, input, label, card, alert, form are installed under src/shared/ui/ and import without errors"
    - "D-03: RESEND_API_KEY is present in .env.local.example (single key for all environments, no test/live split)"
  artifacts:
    - path: "package.json"
      provides: "pnpm scripts (dev, build, type-check, lint, test) and exact dependency versions from RESEARCH.md"
      contains: "next: 16"
    - path: "tsconfig.json"
      provides: "TypeScript strict mode + path alias @/*"
      contains: "\"strict\": true"
    - path: "eslint.config.js"
      provides: "Flat config: module-boundary rule (no deep @/modules imports) + service-role gating for @/shared/db/admin"
      contains: "@/shared/db/admin"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 CSS-first config with @theme tokens for brand colors and shadcn variables"
      contains: "@import \"tailwindcss\""
    - path: "src/i18n.ts"
      provides: "next-intl routing with English-only, localePrefix: 'never'"
      contains: "localePrefix: 'never'"
    - path: "src/app/(office)/layout.tsx"
      provides: "Office route group layout"
      contains: "export default"
    - path: "src/app/(portal)/layout.tsx"
      provides: "Portal route group layout"
      contains: "export default"
    - path: "src/app/scan/page.tsx"
      provides: "Explicit /scan URL stub (NOT a route group)"
      contains: "export default"
    - path: "components.json"
      provides: "shadcn config pointing tailwind.css to app/globals.css with cssVariables: true"
      contains: "globals.css"
  key_links:
    - from: "src/app/(office)/page.tsx"
      to: "src/app/(office)/sign-in/page.tsx (created in plan 05)"
      via: "redirect via next/navigation"
      pattern: "redirect.*sign-in"
    - from: "src/app/globals.css"
      to: "src/shared/ui/button.tsx (and other shadcn primitives)"
      via: "CSS variables --primary, --background consumed by shadcn classes"
      pattern: "var\\(--primary\\)|bg-primary"
---

<objective>
Bootstrap the Next.js 16 + TypeScript strict + Tailwind v4 + shadcn/ui + next-intl repo with the canonical folder structure from docs/DESIGN.md §2.3 and the ESLint rules from docs/DESIGN.md §2.4. This plan delivers INFRA-01 (repo scaffold) and INFRA-07 partial (ESLint rules; CI lands in plan 06).

Purpose: Establish the foundation every subsequent plan builds on. After this plan, `pnpm dev` boots a working Next.js app with route groups for office/portal/scan, Tailwind v4 renders, shadcn primitives mount, and ESLint enforces module boundaries.

Output: A booting Next.js 16 application with all infrastructure files for routing, styling, type safety, and lint gating. No business logic, no DB, no auth wiring (all in later plans).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/DESIGN.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<interfaces>
<!-- Pinned versions from 01-RESEARCH.md §Standard Stack (verified npm 2026-05-01) -->

Core dependencies (exact versions):
- next@16.2.4
- typescript@6.0.3 (devDependency)
- tailwindcss@4.2.4 (devDependency)
- @supabase/ssr@0.10.2
- @supabase/supabase-js@2.105.1
- zod@4.4.2
- next-intl@4.11.0
- @upstash/ratelimit@2.0.8
- @upstash/redis@1.37.0
- @sentry/nextjs@10.51.0
- resend@6.12.2

Dev dependencies (exact versions):
- vitest@4.1.5
- @vitejs/plugin-react (latest compatible)
- @testing-library/react (latest)
- @testing-library/jest-dom (latest)
- madge@8.0.0
- @typescript-eslint/eslint-plugin@8.59.1
- @typescript-eslint/parser@8.59.1
- eslint@10.3.0
- eslint-config-next@16.2.4

shadcn primitives to install (per 01-UI-SPEC.md §Registry Safety):
button input label card alert form

JWT app_metadata claim shape (referenced from auth helpers in plan 04):
{
  tenant_id: string (UUID),
  audience: 'staff_office' | 'staff_shop' | 'customer',
  role: 'admin' | 'manager' | 'office' | 'shop',
  staff_id?: string (UUID),
  workstation_id?: string (UUID),
  company_id?: string (UUID)
}

Allowed @/shared/db/admin import locations (per CLAUDE.md):
src/modules/settings/**, src/modules/portal/**, src/modules/auth/**, src/shared/audit/**, supabase/functions/**
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Initialize Next.js 16 + TypeScript strict + Tailwind v4 + dependencies + folder structure</name>
  <files>
    package.json,
    pnpm-lock.yaml,
    tsconfig.json,
    next.config.ts,
    .gitignore,
    .env.local.example,
    src/app/layout.tsx,
    src/app/globals.css,
    src/app/(office)/layout.tsx,
    src/app/(office)/page.tsx,
    src/app/(portal)/layout.tsx,
    src/app/(portal)/page.tsx,
    src/app/scan/page.tsx,
    src/i18n.ts,
    src/messages/en/common.json,
    src/messages/en/auth.json
  </files>
  <read_first>
    - CLAUDE.md (stack constraints, hidden invariants — Section "Stack constraints")
    - .planning/phases/01-foundation/01-RESEARCH.md (lines 86–135 for exact pinned versions; lines 187–260 for folder structure; lines 885–926 for Tailwind v4 + i18n config)
    - .planning/phases/01-foundation/01-PATTERNS.md (Sections "src/app/globals.css" and "src/i18n.ts" for CSS and routing config)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Sections "Color" and "Typography" for the EXACT CSS variable values)
    - docs/DESIGN.md §2.3 (lines 104–179 for canonical folder structure)
  </read_first>
  <action>
    Initialize the repo using pnpm only (per D-12 in CLAUDE.md). Run:

    ```
    pnpm create next-app@16 . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
    ```

    If the CLI prompts, accept defaults; ensure src/ directory is used. Then install the EXACT versions from the interfaces block above (per 01-RESEARCH.md §Standard Stack — verified 2026-05-01):

    ```
    pnpm add next@16.2.4 @supabase/ssr@0.10.2 @supabase/supabase-js@2.105.1 zod@4.4.2 next-intl@4.11.0 @upstash/ratelimit@2.0.8 @upstash/redis@1.37.0 @sentry/nextjs@10.51.0 resend@6.12.2
    pnpm add -D typescript@6.0.3 tailwindcss@4.2.4 vitest@4.1.5 @vitejs/plugin-react @testing-library/react @testing-library/jest-dom madge@8.0.0 @typescript-eslint/eslint-plugin@8.59.1 @typescript-eslint/parser@8.59.1 eslint@10.3.0 eslint-config-next@16.2.4
    ```

    DELETE any auto-generated `tailwind.config.ts` or `tailwind.config.js` (Tailwind v4 is CSS-first per CLAUDE.md and 01-RESEARCH.md Pitfall 7).

    Edit `tsconfig.json` to ensure `"strict": true` is present in compilerOptions, plus `"noUncheckedIndexedAccess": true`, `"noImplicitOverride": true`, and the path alias `"@/*": ["./src/*"]`. NO `any` allowed; NO non-null `!` assertions without justification (per CLAUDE.md).

    Edit `package.json` to add scripts:
    ```json
    {
      "scripts": {
        "dev": "next dev",
        "build": "next build",
        "start": "next start",
        "type-check": "tsc --noEmit",
        "lint": "next lint && pnpm madge:check",
        "madge:check": "madge --circular src/modules",
        "test": "vitest run",
        "test:watch": "vitest"
      }
    }
    ```

    Create `next.config.ts` with `experimental: { typedRoutes: true }` and `poweredByHeader: false`. Wire `next-intl/plugin` if needed for i18n message loading.

    Create the canonical folder structure (per docs/DESIGN.md §2.3 + 01-RESEARCH.md lines 187–260):

    ```
    src/
      app/
        layout.tsx                          (root layout: html, body, font, next-intl provider)
        globals.css                         (Tailwind v4 + @theme + shadcn CSS variables)
        (office)/
          layout.tsx                        (office route group layout — staff app.popsindustrial.com)
          page.tsx                          (Phase 1: redirect to /sign-in)
        (portal)/
          layout.tsx                        (portal route group layout — customer track.popsindustrial.com)
          page.tsx                          (Phase 1: redirect to /sign-in)
        scan/                               (NOT a route group — explicit /scan URL per CLAUDE.md)
          page.tsx                          (Phase 1: stub returning a placeholder)
      i18n.ts
      messages/
        en/
          common.json
          auth.json
    ```

    `src/app/(office)/page.tsx` — `import { redirect } from 'next/navigation'; export default function OfficeHome() { redirect('/sign-in') }`.
    `src/app/(portal)/page.tsx` — `import { redirect } from 'next/navigation'; export default function PortalHome() { redirect('/sign-in') }`.
    `src/app/scan/page.tsx` — minimal stub: `export default function ScanPage() { return <main>Scanner stub — Phase 3</main> }` (Phase 3 ships the real UI per CONTEXT.md D-04).

    `src/app/globals.css` — write EXACTLY the CSS from 01-UI-SPEC.md §Color (lines 88–107) PLUS the @theme block from 01-RESEARCH.md lines 887–897. Concretely:

    ```css
    @import "tailwindcss";

    @theme {
      --font-sans: Inter, ui-sans-serif, system-ui, sans-serif;
      --color-brand-primary: oklch(0.6 0.2 250);
      --color-brand-secondary: oklch(0.8 0.1 250);
    }

    @layer base {
      :root {
        --background: 210 40% 98%;           /* #F8FAFC */
        --foreground: 222 41% 20%;           /* #1E2D4E */
        --card: 0 0% 100%;                   /* #FFFFFF */
        --card-foreground: 222 41% 20%;      /* #1E2D4E */
        --muted: 215 28% 17%;                /* #374B6D */
        --muted-foreground: 215 28% 32%;     /* #374B6D */
        --primary: 38 93% 51%;               /* #F59E0B */
        --primary-foreground: 222 41% 20%;   /* dark navy on amber = 5.2:1 AA */
        --destructive: 0 72% 51%;            /* #DC2626 */
        --destructive-foreground: 0 0% 100%;
        --border: 214 32% 91%;
        --input: 214 32% 91%;
        --ring: 38 93% 51%;                  /* #F59E0B focus ring */
        --radius: 0.5rem;
      }
      .dark {
        --background: 222.2 84% 4.9%;
        --foreground: 210 40% 98%;
      }
    }
    ```

    `src/i18n.ts` — write EXACTLY (per 01-RESEARCH.md lines 914–926, CONTEXT.md English-only Wave 1):
    ```typescript
    import { defineRouting } from 'next-intl/routing'

    export const routing = defineRouting({
      locales: ['en'],
      defaultLocale: 'en',
      localePrefix: 'never',
    })
    ```

    `src/messages/en/common.json` — minimal stub: `{ "app_name": "Pops Industrial Coatings" }`.
    `src/messages/en/auth.json` — minimal stub: `{ "sign_in_heading": "Sign in", "sign_in_button": "Sign In", "error_invalid_credentials": "The email or password you entered is not correct. Please try again.", "error_account_inactive": "Your account is not active. Contact your administrator.", "error_magic_link_expired": "This link has expired. Request a new one.", "error_network": "Something went wrong. Please check your connection and try again." }` (copy strings from 01-UI-SPEC.md §Copywriting Contract verbatim).

    Create `.env.local.example` with placeholders for env vars used by later plans:
    ```
    NEXT_PUBLIC_SUPABASE_URL=https://<project>.supabase.co
    NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-key>
    SUPABASE_SERVICE_ROLE_KEY=<service-role-key>
    UPSTASH_REDIS_REST_URL=
    UPSTASH_REDIS_REST_TOKEN=
    SENTRY_DSN=
    SENTRY_AUTH_TOKEN=
    RESEND_API_KEY=
    SUPABASE_ACCESS_TOKEN=
    ```

    Add to `.gitignore` (in addition to Next.js defaults): `.env*.local`, `.vercel`, `coverage/`, `.next/`, `node_modules/`, `*.tsbuildinfo`, `.sentryclirc`.

    Verify scaffold boots: `pnpm dev` should start without errors (kill after confirming).
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile && pnpm type-check &amp;&amp; test -f src/app/globals.css &amp;&amp; test -f src/i18n.ts &amp;&amp; test -f src/app/scan/page.tsx &amp;&amp; test -f src/app/(office)/layout.tsx &amp;&amp; test -f src/app/(portal)/layout.tsx &amp;&amp; ! test -f tailwind.config.ts &amp;&amp; ! test -f tailwind.config.js &amp;&amp; grep -q '"strict": true' tsconfig.json &amp;&amp; grep -q '"next": "16' package.json &amp;&amp; grep -q "@import \"tailwindcss\"" src/app/globals.css &amp;&amp; grep -q "localePrefix: 'never'" src/i18n.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `package.json` contains `"next": "16.2.4"` (or compatible 16.x)
    - File `package.json` contains `"@supabase/ssr": "0.10.2"` (or compatible 0.10.x)
    - File `package.json` contains `"next-intl": "4.11.0"` (or compatible)
    - File `package.json` contains `"madge": "8.0.0"` (or compatible)
    - File `package.json` scripts section contains `"type-check": "tsc --noEmit"`, `"lint": "next lint && pnpm madge:check"`, `"madge:check": "madge --circular src/modules"`, `"test": "vitest run"`
    - File `tsconfig.json` contains `"strict": true`
    - File `tsconfig.json` contains `"noUncheckedIndexedAccess": true`
    - No file `tailwind.config.ts` exists; no file `tailwind.config.js` exists (Tailwind v4 is CSS-first per CLAUDE.md and 01-RESEARCH.md Pitfall 7)
    - File `src/app/globals.css` contains `@import "tailwindcss"`
    - File `src/app/globals.css` contains `@theme {`
    - File `src/app/globals.css` contains `--primary: 38 93% 51%;` (the amber #F59E0B from UI-SPEC)
    - File `src/i18n.ts` contains `localePrefix: 'never'`
    - File `src/app/scan/page.tsx` exists (NOT under a route group — explicit /scan URL per CLAUDE.md)
    - File `src/app/(office)/layout.tsx` exists
    - File `src/app/(portal)/layout.tsx` exists
    - File `src/app/(office)/page.tsx` contains `redirect('/sign-in')`
    - File `src/app/(portal)/page.tsx` contains `redirect('/sign-in')`
    - File `src/messages/en/auth.json` contains `"The email or password you entered is not correct. Please try again."` (anti-enumeration copy from UI-SPEC §Copywriting Contract)
    - File `.env.local.example` contains `NEXT_PUBLIC_SUPABASE_URL=`
    - File `.env.local.example` contains `RESEND_API_KEY=`
    - File `.gitignore` contains `.env*.local`
    - Command `pnpm install --frozen-lockfile` exits 0
    - Command `pnpm type-check` exits 0
  </acceptance_criteria>
  <done>
    Repo scaffold is in place: pnpm install + pnpm type-check both pass, the canonical folder structure exists, Tailwind v4 CSS-first config is in src/app/globals.css with the exact UI-SPEC color tokens, next-intl is configured English-only with no URL prefix, route groups for (office)/(portal) and the explicit /scan path are stubbed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Configure ESLint flat config (module boundaries + service-role gating) and install shadcn/ui primitives</name>
  <files>
    eslint.config.js,
    components.json,
    src/shared/ui/button.tsx,
    src/shared/ui/input.tsx,
    src/shared/ui/label.tsx,
    src/shared/ui/card.tsx,
    src/shared/ui/alert.tsx,
    src/shared/ui/form.tsx
  </files>
  <read_first>
    - CLAUDE.md (Stack constraints — service-role gating list and hidden invariants)
    - .planning/phases/01-foundation/01-RESEARCH.md (lines 438–477 for ESLint flat config pattern; lines 1054 for full service-role module list)
    - .planning/phases/01-foundation/01-PATTERNS.md (Section "eslint.config.js" — both mandatory rule blocks)
    - docs/DESIGN.md §2.4 (lines 180–214 for module communication rules and ESLint pattern)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Section "Registry Safety" — exact shadcn components list)
    - src/app/globals.css (created in Task 1 — needed by shadcn init for cssVariables wiring)
  </read_first>
  <action>
    Create `eslint.config.js` (flat config — eslint v10 + @typescript-eslint v8 per RESEARCH.md State of the Art) with BOTH mandatory rule blocks. Write EXACTLY (per 01-PATTERNS.md Section "eslint.config.js" + 01-RESEARCH.md lines 438–477 + CLAUDE.md service-role gating list):

    ```javascript
    import nextPlugin from 'eslint-config-next'
    import tsPlugin from '@typescript-eslint/eslint-plugin'
    import tsParser from '@typescript-eslint/parser'

    /** @type {import('eslint').Linter.Config[]} */
    const config = [
      ...nextPlugin,
      // Block 1: Module boundary — deep imports forbidden, must import from index.ts
      {
        rules: {
          'no-restricted-imports': ['error', {
            patterns: [{
              group: ['@/modules/*/!(index)*'],
              message: 'Import from module index.ts only (CLAUDE.md module communication rules)',
            }],
          }],
          '@typescript-eslint/no-explicit-any': 'error',
          '@typescript-eslint/no-non-null-assertion': 'error',
        },
      },
      // Block 2: Service-role gating — @/shared/db/admin restricted to allowed modules only
      // Allowed: src/modules/{settings,portal,auth}/**, src/shared/audit/**, supabase/functions/**
      // Forbidden: src/modules/scanning/** and all other modules
      {
        files: [
          'src/modules/!(settings|portal|auth)/**',
          'src/shared/!(audit|auth-helpers|db)/**',
          'src/app/**',
          'src/proxy.ts',
        ],
        rules: {
          'no-restricted-imports': ['error', {
            paths: [{
              name: '@/shared/db/admin',
              message: 'service-role import not allowed in this module (CLAUDE.md service-role gating)',
            }],
          }],
        },
      },
    ]

    export default config
    ```

    NOTE on glob: per CLAUDE.md the allowed list is exactly `src/modules/{settings,portal,auth}/**`, `src/shared/audit/**`, `supabase/functions/**`. The negation glob `!(settings|portal|auth)` covers modules; for shared/ we exclude `audit`, `auth-helpers` (which calls server.ts), `db` (defines admin.ts itself); proxy.ts and src/app/** must NOT import admin (per the same gating rule).

    Initialize shadcn/ui targeting Tailwind v4. Run:

    ```
    pnpm dlx shadcn@latest init
    ```

    When prompted: choose "Default" style, base color "Slate", `tailwind.css` path = `src/app/globals.css`, CSS variables = YES (per 01-RESEARCH.md Pitfall 7 and 01-UI-SPEC.md). If the CLI generates a `tailwind.config.ts`, DELETE it (Tailwind v4 is CSS-first). The `components.json` MUST point to `src/app/globals.css` and have `cssVariables: true`.

    Verify `components.json` content includes:
    - `"style": "default"` (or whatever the current default; not "new-york" unless intentional)
    - `"tailwind": { "css": "src/app/globals.css", "cssVariables": true }`
    - `"aliases": { "components": "@/shared/ui", "utils": "@/lib/utils" }` — note components alias must be `@/shared/ui` (per docs/DESIGN.md §4.4 shared utilities tree)

    Install the EXACT shadcn primitives required by Phase 1 (per 01-UI-SPEC.md §Registry Safety):

    ```
    pnpm dlx shadcn@latest add button input label card alert form
    ```

    These should land in `src/shared/ui/` (button.tsx, input.tsx, label.tsx, card.tsx, alert.tsx, form.tsx). If shadcn places them elsewhere (e.g. `src/components/ui/`), MOVE them to `src/shared/ui/` and update `components.json` aliases to match.

    After install, run `pnpm type-check` to confirm shadcn primitives compile against TypeScript strict.
  </action>
  <verify>
    <automated>test -f eslint.config.js &amp;&amp; test -f components.json &amp;&amp; test -f src/shared/ui/button.tsx &amp;&amp; test -f src/shared/ui/input.tsx &amp;&amp; test -f src/shared/ui/label.tsx &amp;&amp; test -f src/shared/ui/card.tsx &amp;&amp; test -f src/shared/ui/alert.tsx &amp;&amp; test -f src/shared/ui/form.tsx &amp;&amp; grep -q "@/shared/db/admin" eslint.config.js &amp;&amp; grep -q "@/modules/\*/!(index)\*" eslint.config.js &amp;&amp; grep -q "src/app/globals.css" components.json &amp;&amp; grep -q "cssVariables" components.json &amp;&amp; pnpm type-check &amp;&amp; pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - File `eslint.config.js` exists at repo root
    - File `eslint.config.js` contains the string `@/shared/db/admin`
    - File `eslint.config.js` contains the string `@/modules/*/!(index)*` (module boundary glob)
    - File `eslint.config.js` contains the string `'@typescript-eslint/no-explicit-any': 'error'` (no `any` per CLAUDE.md)
    - File `eslint.config.js` contains the string `'@typescript-eslint/no-non-null-assertion': 'error'` (no `!` per CLAUDE.md)
    - File `components.json` contains the string `src/app/globals.css`
    - File `components.json` contains `"cssVariables": true`
    - File `components.json` contains `"@/shared/ui"` (components alias)
    - File `src/shared/ui/button.tsx` exists and exports `Button` (`grep -c "export.*Button" src/shared/ui/button.tsx` returns at least 1)
    - File `src/shared/ui/input.tsx` exists
    - File `src/shared/ui/label.tsx` exists
    - File `src/shared/ui/card.tsx` exists
    - File `src/shared/ui/alert.tsx` exists
    - File `src/shared/ui/form.tsx` exists
    - No `tailwind.config.ts` or `tailwind.config.js` was generated by shadcn init (Tailwind v4 CSS-first per CLAUDE.md)
    - Command `pnpm type-check` exits 0
    - Command `pnpm lint` exits 0 (with no warnings or errors)
  </acceptance_criteria>
  <done>
    ESLint flat config enforces module boundaries (no deep `@/modules/*/...` imports) AND service-role gating (`@/shared/db/admin` blocked outside allowed modules). shadcn/ui is initialized for Tailwind v4 CSS-first; the 6 Phase 1 primitives (button, input, label, card, alert, form) are installed in `src/shared/ui/`; `pnpm lint` passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → npm registry | Untrusted package downloads (supply chain) |
| Local source code → ESLint | ESLint enforces import boundaries that protect against developer mistakes leaking service-role into client code |
| `.env.local` → committed git history | Secrets in env files must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01-01 | Tampering | Dependency supply chain | Medium | mitigate | Pin EXACT versions from RESEARCH.md (verified 2026-05-01); use `pnpm install --frozen-lockfile`; commit pnpm-lock.yaml |
| T-01-01-02 | Information Disclosure | `.env.local` accidentally committed | High | mitigate | `.gitignore` includes `.env*.local`; `.env.local.example` with placeholders only (no real values) |
| T-01-01-03 | Elevation of Privilege | Service-role import leaks into a non-allowed module (e.g., scanning, dashboard) | High | mitigate | ESLint flat config Block 2 errors on `@/shared/db/admin` import outside `src/modules/{settings,portal,auth}/**` and `src/shared/audit/**`; CI fails on `pnpm lint` (Plan 06) |
| T-01-01-04 | Tampering | Deep module imports bypass `index.ts` API surface | Medium | mitigate | ESLint flat config Block 1 errors on `@/modules/*/!(index)*`; madge --circular catches transitive cycles in CI (Plan 06) |
| T-01-01-05 | Spoofing | TypeScript `any` or `!` assertion bypasses strict mode invariants | Medium | mitigate | `@typescript-eslint/no-explicit-any: error` + `@typescript-eslint/no-non-null-assertion: error` in eslint.config.js |
| T-01-01-06 | Information Disclosure | `tailwind.config.ts` accidentally created (Pitfall 7 in RESEARCH.md) leaks Tailwind v3 config patterns into v4 stack and breaks shadcn theming | Low | mitigate | Acceptance criteria assert no `tailwind.config.ts` or `tailwind.config.js` exists; deleted post-shadcn-init if generated |

Residual risks: Supply-chain compromise of npm packages between version pin and install (mitigated by pnpm-lock.yaml hash verification); developer manually overriding ESLint with `eslint-disable` comments (caught in code review).
</threat_model>

<verification>
1. `pnpm install --frozen-lockfile` succeeds (lockfile is present and consistent)
2. `pnpm type-check` succeeds (TypeScript strict passes across all files including shadcn primitives)
3. `pnpm lint` succeeds (no ESLint errors; module-boundary and service-role rules are loaded)
4. `pnpm dev` starts without errors (manual smoke check during implementation; not part of automated verify)
5. `grep -q "@/shared/db/admin" eslint.config.js` confirms service-role gating is wired
6. `! test -f tailwind.config.ts` confirms Tailwind v4 CSS-first is enforced
</verification>

<success_criteria>
- pnpm install completes from a clean checkout using `--frozen-lockfile`
- pnpm type-check passes (TypeScript strict)
- pnpm lint passes (ESLint flat config enforces module boundaries + service-role gating + no any + no !)
- src/app/globals.css contains all 13 CSS variables specified in 01-UI-SPEC.md §Color
- components.json points to src/app/globals.css with cssVariables: true and @/shared/ui alias
- All 6 shadcn primitives exist in src/shared/ui/ and compile under TypeScript strict
- Folder structure matches docs/DESIGN.md §2.3 with route groups for (office)/(portal) and explicit /scan path
- No `tailwind.config.ts` or `tailwind.config.js` exists anywhere in repo
- next-intl is configured with `locales: ['en']` and `localePrefix: 'never'` (English-only Wave 1 per CLAUDE.md)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- Pinned versions installed (with any deviations from RESEARCH.md and why)
- Confirmation that no `tailwind.config.*` was generated
- Output of `components.json` for downstream plans to reference
- Any shadcn primitives that landed outside `src/shared/ui/` and were moved
</output>

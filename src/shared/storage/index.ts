// src/shared/storage/index.ts
// Phase 1 stub. Full Storage upload helpers land in Phase 3 (SCAN-05).

// TODO(Phase 3 SCAN-05): replace with signed URL generator using Supabase Storage.
export async function getSignedUrl(_bucket: string, _path: string): Promise<string> {
  return ''
}

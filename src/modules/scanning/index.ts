// Phase 3 module. Public API will export: recordScanEvent, claimWorkstation,
// releaseWorkstation, recordWorkstationHeartbeat — see docs/DESIGN.md §4.3 Module 5 (Scanning).
// Note: service-role import is FORBIDDEN in this module (CLAUDE.md).
// Uses @zxing/browser for camera-based QR scanning on iPad Safari.
export {}

// Phase 2 module. Public API will export: generateJobPacket, markPacketDirty,
// getPacketPDF — see docs/DESIGN.md §4.3 Module 4 (Packets).
// Uses @react-pdf/renderer + qrcode for QR-code PDF generation.
export {}
